import { Multiply } from "./Index";

describe("Sum", () => {
  describe("Multiply", () => {
    it("Should return the product of the input", () => {
      expect(Multiply(3, 2)).toBe(6);
    });
  });
});
